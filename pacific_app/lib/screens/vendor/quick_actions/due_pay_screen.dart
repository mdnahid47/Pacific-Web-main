import 'package:flutter/material.dart';

class DuePayScreen extends StatelessWidget {
  const DuePayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Due & Payments')),
      body: const Center(child: Text('Due & Payments Screen')),
    );
  }
}
